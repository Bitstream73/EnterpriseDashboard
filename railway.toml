[build]
builder = "NIXPACKS"

[deploy]
startCommand = "dotenvx run -- node server.js"
healthcheckPath = "/api/health"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
